#include<iostream>
#include<algorithm>
using namespace std;


int main()
{
    int arr[]={2,6,3,9,5,0};
   
    int size=sizeof(arr)/sizeof(arr[0]);
    sort(arr,arr+size);
    cout<<"\n";
    for(int i=0;i<size;i++)
    {
        cout<<arr[i];
    }
    return 0;
}
